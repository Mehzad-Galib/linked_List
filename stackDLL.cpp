#include <bits/stdc++.h>
using namespace std;

class Node
{
public:
    int val;
    Node *next;
    Node *prev;
    Node(int val)
    {
        val = val;
        next = NULL;
        prev = NULL;
    }
};

class stack
{
    Node *head;
    Node *top;

public:
    // push operation
    void push(int val)
    {
        if (head == NULL)
        {
        }
        else
        {
        }
    }
    // pop operation
    // empty, size, top
};

int main()
{
}